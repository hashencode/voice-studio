# Consented Chinese-English code-switch recording script

Assign each speaker and session wholly to development or held-out use. Preserve
the intended English tokens in the reference and annotate Chinese and English
spans separately.

Suggested prompts:

- 请把 release candidate 部署到 staging environment，再运行 smoke test。
- 今天的 stand-up 重点是 latency、memory usage 和 cancellation。
- 如果 API response 超过两秒，就触发 fallback policy。
- 我们需要 review pull request，并确认 macOS arm64 build 通过。
- 客户说 transcription quality 很好，但 punctuation 还要改进。

Consent records, raw audio, speaker identity, and reviewed references remain
local-only.
