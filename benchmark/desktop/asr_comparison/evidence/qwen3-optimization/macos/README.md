# Qwen3 optimization evidence

This directory accepts only hash-bound Mac mini `Mac16,10` / Apple M4 /
16 GiB / macOS 15.7.5 (24G624) evidence generated under
`qwen3_optimization_contract.json`.

The fixed English, code-switch, far-field/noise, terminology/numbers,
system+microphone double-talk, and non-speech inputs were supplied as a
hash-bound local quality pack. The bounded runner completed all eight
preregistered arms and atomically wrote `matrix.json`; the finalizer retained
only raw records bound to the current contract and produced the mutually bound
raw manifest, summary, and decision.

The machine decision is `OPTIMIZATION_ADMITTED` with
`vad-max-speech-12`. `raw/quarantine/` retains fail-closed earlier attempts but
is excluded from formal evidence. Historical observations using another
fixture, scorer, runtime, or contract remain diagnostic context only.
